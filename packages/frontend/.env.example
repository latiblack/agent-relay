# Agent Relay - Frontend Environment Variables
# Copy to .env and fill in values
# Only VITE_ prefixed vars are exposed to the client

# Relay server URL (your backend)
VITE_RELAY_SERVER=http://localhost:3104
